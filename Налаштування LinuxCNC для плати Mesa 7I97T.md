# Налаштування LinuxCNC для плати Mesa 7I97T

Покроковий гайд з підключення та конфігурації Ethernet-контролера Mesa 7I97T для LinuxCNC.

> **Важливо:** стандартний PnCconf (у стабільних релізах LinuxCNC) не підтримує 7I97T напряму — плати немає в списку. Робочі шляхи: **mesact** (рекомендовано), ручна правка HAL/INI на основі зразків від Mesa, або експериментальна гілка PnCconf для LinuxCNC 2.10.

## Зміст

- [Вимоги](#вимоги)
- [1. Оновлення LinuxCNC](#1-оновлення-linuxcnc)
- [2. Фізичне підключення](#2-фізичне-підключення)
- [3. Встановлення утиліт](#3-встановлення-утиліт)
- [4. Перевірка зв'язку та вибір прошивки](#4-перевірка-звязку-та-вибір-прошивки)
- [5. Генерація конфігурації](#5-генерація-конфігурації)
- [6. Налаштування аналогових виходів (серво)](#6-налаштування-аналогових-виходів-серво)
- [7. Перевірка без увімкнених приводів](#7-перевірка-без-увімкнених-приводів)
- [8. Увімкнення приводів і тюнінг](#8-увімкнення-приводів-і-тюнінг)
- [Корисні посилання](#корисні-посилання)

## Вимоги

- Плата Mesa 7I97T
- ПК з LinuxCNC 2.9+ (краще master-гілка 2.10)
- Вільний Ethernet-порт для прямого з'єднання з платою
- Живлення: 5V на P3 (логіка), 24V на TB4 (I/O)

## 1. Оновлення LinuxCNC

7I97T потребує сучасних Mesa-драйверів (`hm2_eth`), яких немає у версії 2.7.

- Мінімум — **2.8**
- Рекомендовано — **2.9** або **master (2.10)**, де найкраща підтримка через `mesact`

Якщо стоїть стара збірка — додайте репозиторій Buildbot з потрібною версією і оновіться.

## 2. Фізичне підключення

- З'єднайте 7I97T з ПК окремим Ethernet-кабелем (бажано напряму, без свіча — менша затримка)
- Подайте **5V на P3** (логіка) та окремо **24V на TB4** (I/O-кола)
- За замовчуванням плата має статичну IP (зазвичай `192.168.1.121`) — переконайтесь, що мережева карта ПК у тій самій підмережі

## 3. Встановлення утиліт

| Утиліта | Призначення |
|---|---|
| `mesaflash` | Діагностика й прошивка плати (консоль) |
| `mesact` | Графічний конфігуратор нового покоління, коректно генерує HAL/INI для 7I97T |

Встановіть свіжу версію `mesaflash` (не нижче **3.5.9**) — старіші версії не завжди коректно працюють із цією платою.

## 4. Перевірка зв'язку та вибір прошивки

```bash
mesaflash --device 7i97t --readhmid
```

Якщо плата відповідає — зв'язок є. Далі через `mesact` або `mesaflash` оберіть прошивку (firmware) відповідно до вашої конфігурації: кількість енкодерів, PWM/аналогових виходів, степ/дір-генераторів — залежно від того, серво чи крокові двигуни підключаєте.

## 5. Генерація конфігурації

**Варіант A (рекомендовано): `mesact`**
Проводить через вибір плати, портів вводу/виводу, осей і типу приводів, генерує робочі HAL/INI-файли.

**Варіант B: зразки від Mesa**
Взяти базові sample-файли для 7I97T і адаптувати вручну (перепризначити піни під своє обладнання).

**Варіант C: експериментальний PnCconf**
Потрібна окрема гілка для LinuxCNC 2.10 (шукайте на forum.linuxcnc.org) — стандартний PnCconf карту не знає.

## 6. Налаштування аналогових виходів (серво)

Якщо у вас серводвигуни з аналоговим керуванням (не крокові), обов'язково увімкніть offset-mode для PWM-генераторів:

```
setp [HMOT](CARD0).pwmgen.NN.offset-mode 1
```

Це забезпечує, що 50% шпаруватості відповідає 0V — необхідно для біполярних сервопідсилювачів. Дотримуйтесь рекомендацій щодо PWM-частоти з мануалу 7I97T для найкращого співвідношення шум/роздільність.

## 7. Перевірка без увімкнених приводів

**Перш ніж вмикати drive enable**, перевірте:

- [ ] Енкодери показують правильний напрямок і масштаб (оберти валу відповідають очікуваним показникам)
- [ ] Сигнали enable коректно вмикаються/вимикаються в HAL
- [ ] Виставлені розумні ліміти follow-error для подальшого тюнінгу

> ⚠️ На аналогових виходах є ~50% шанс переплутаної полярності зворотного зв'язку. З увімкненим приводом це призведе до runaway (вісь піде вразнос без контролю).

## 8. Увімкнення приводів і тюнінг

Після перевірки п.7 можна вмикати enable на приводах.

- Якщо вісь одразу йде в один бік без керування → неправильна полярність аналогового виходу або зворотного зв'язку з енкодера. Поверніть полярність у HAL і повторіть тест.
- Далі — стандартний тюнінг PID (**P, I, FF1, FF2**) через Halscope або Halshow, як для будь-якої сервосистеми LinuxCNC.

## Корисні посилання

- [LinuxCNC Forum — Driver Boards](https://forum.linuxcnc.org) (гілки по 7i97/7i97t retrofit)
- Мануал Mesa 7I97T (Mesa Electronics) — точні номери пінів вашого розводу I/O

---

*Якщо цей гайд вам допоміг або ви знайшли неточність — issue/PR вітаються.*
